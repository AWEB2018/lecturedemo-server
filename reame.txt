## DAY_LAST
    CREATE TABLE POSTS (
        ID int NOT NULL AUTO_INCREMENT,
        LIKE_COUNT NUMBER(11) NOT NULL,
        DISLIKE_COUNT varchar(255),
        Age int,
        PRIMARY KEY (ID)
    );

## DAY 14
    >> ALLOW CROSS ORIGIN REQUEST
        npm install cors --save
        app.use(cors);

    >> ExpressJS    
        >>> Start the server
        >>> Create basic get service.

    >> NODEJS AND MYSQL INTEGRATION
        >>> START YOUR DATABASE
        >>> CREATE DATABASE (DAC2018)
        >>> CREATE TABLE (EMPLOYEE)
    
    >> AUTHORIZATION
        >>> URL of your DATABASE
        >>> USERNAME :: root
        >>> PASSWORD :: ""

    >> NOTE::
        >>> Write all your sql in one file. project.sql

    >> INSTALL node library. :: IMPORTANT
        >>> npm install mysql --save

    >> FIRST PRGORAM
        >>> Verify/Check datbase connection.


## DAY 13
    >> Overloading Concept in JS/PHP.
    >> Callback Concept and practice.
    >> Read File and Write File. 
    >> Promise Concept. 

    >> ExpressJS and start a server. 
        >>> npm install express --save
    
    >> Create your first angular js project.
        npm install -g @angular/cli
        ng new my-app

        npm install --save-dev typescript@2.6.2
        ng serve



## DAY 12
    >> SETUP PROJECT
        >>> npm init
        >>> npm install typescript -g
        >>> tsc --init
        >>> Modify the tsconfig.json file. 
            >>>> allowjs : true
            >>>> sourcemap : true
            >>>> outDir : './bin'       // Create bin folder
            >>>> rootDir : './src'      // Create src folder
        >>> Update Package.json file
            >>>> "dev" : "tsc -w -p tsconfig.json"
    >> Setup Debugging. 


## ASSIGNMENTS
    >> Create First Class. Print Hello World.
    >> Create static and non static method.

    >> Create constructor of a class.
    >> Export a module. *** module.exports
    >> Import a module. *** require

    

    
